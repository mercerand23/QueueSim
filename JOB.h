#ifndef myJOB#define myJOB//----------------------------------------------------------------------//  SPECIFICATION FILE (JOB.h)//  This module declares a new type for ONE JOB//----------------------------------------------------------------------#include "constants.h"using namespace std;#include <iostream>class JOB{public:	JOB (const JOB& job);	JOB();                  // Default CTOR	JOB(long useThisId);   // CTOR when ID is known; ID# is passed in	JOB(long useThisId, string name, double totalTime, double remainingTime);	friend ostream& operator<< (ostream& os, const JOB& obj);	//Setters	void setId(long useThisId);	void setRemainingTime(double newTime);	//Getters	long getId();	string getName();	double getRemainingTime();	double getTotalTime();	double randJobTime(long JobType);	JOB getNextJob();	JOB addJOB(JOB& job);private:	string name;        // TYPE of process	// you need to add more to this class declaration	long id;	double totalTime; //base time (not changed)	double remainingTime; //Time left before it is removed from the queue};  // end class JOB#endif